import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function CartShopping(){
    return(
        <SafeAreaView>
            <Text>CardShopping</Text>
        </SafeAreaView>
    )
}